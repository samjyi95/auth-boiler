<h1><%= user.firstname %> Visiting <%= userProfile.name %>'s Page! </h1>
<!-- this page would allow a user to view someone's page but not allow them to edit pics -->


<div class="row">
    <div class="col s4">
        <img src="<%= userProfile.pic %>" alt="profile pic" style="height: 500px; width: auto;">
        <div>
            <p><strong>Name:</strong> <%= user.firstname %> <%= user.lastname %> </p>
        </div>
            <p>
                <%= user.bio %>
            </p>
    </div>
</div>

<div class="row">
    <div class="col s4">
        <!-- The link below should lead them to that user "that they're visiting/pic/:id". And you should
        probably include the img tag WITHIN the opening <a> tag-->
        <a href="">
            <img src="<%= userProfile.pic %>" alt="profile pic" style="height: 500px; width: auto;">
        </a>
        <div>
            <p><strong>Name:</strong> <%= user.firstname %> <%= user.lastname %> </p>
        </div>
            <p>
                <%= user.bio %>
            </p>
    </div>
</div>